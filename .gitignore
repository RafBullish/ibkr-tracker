# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output
node_modules
dist
dist-ssr
*.local
build
.next
.cache

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.local
.env.*.local
!.env.example
.vercel

# Local dev artifacts
.claude/
.tmp.driveupload/
.tmp.drivedownload/
fichiers.txt
tree.txt
phase-*.txt
phase-c-files.txt
contexte_pour_gemini.md

# Playwright MCP — sessions, snapshots a11y, logs console
.playwright-mcp/

# Captures de vérification visuelle (racine uniquement — assets/public restent traqués)
/dashboard.png
/brique*.png
/nettoyage*.png
/font-*.png
/c3-*.png
/c3-*.zip

# Runs de audit:visual — régénérables via `npm run audit:visual` (docs/captures/c3/
# reste traqué comme historique). On ne commite pas les 12 captures par run (bloat).
docs/captures/audit-*/

# Iosevka source pack (non-subsetté, 1.6 MB chaque) — gardé hors public/ pour ne pas shipper
scripts/iosevka-source/

# Artefacts de verification brique-par-brique (screenshots, scripts jetables)
/.checkpoints/
